/**********************************************************************************************************
 * strlen.c
 * _strlen-finds the length of a string
 * @str-string input
 *
 * return-the length of str
 * **********************************************************************************************************/

#include "main.h"

int _strlen(char *str)
{
	int i;

	for(i=0;str[i]!='\0';i++)
	{
		;
	}

	return(i);
}
